import { StyleSheet } from 'react-native'

export default StyleSheet.create({

    container: {
        padding: 10,
        marginTop: 10,
        borderWidth: 2,
        borderRadius: 8,
        borderColor: '#FFFFFF',
        width: 300,
    },
});